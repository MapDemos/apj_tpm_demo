"""
query分類タスクで共有するカテゴリ定義・プロンプト生成ロジック。

classify_queries.py（プロキシ経由・並行処理版）と
classify_queries_batch.py（Anthropic Message Batches API版）の
両方から import される。番号↔カテゴリの対応をここ1箇所だけに集約することで、
プロンプト文言とPython側マッピングがズレる事故を防ぐ。
"""

# デフォルトモデル。ai-classify系サブコマンドは --model haiku|sonnet で切り替え可能
# （MODEL_CHOICESを参照）。ここは後方互換のためhaikuのまま残す。
MODEL = "claude-haiku-4-5-20251001"

# main.pyの --model 引数(haiku/sonnet)からモデルIDへの変換テーブル
MODEL_CHOICES: dict[str, str] = {
    "haiku": "claude-haiku-4-5-20251001",
    "sonnet": "claude-sonnet-5",
}

# 番号→カテゴリ名。出力トークン削減のため、LLMにはカテゴリ名の文字列ではなく
# この番号（1〜7の整数）だけを返させる。
CATEGORIES: dict[int, str] = {
    1: "poi",
    2: "poi_brand",
    3: "poi_category",
    4: "address",
    5: "unsupported_query_location_intent",
    6: "broken_query",
    7: "others",
}

CATEGORY_DESCRIPTIONS: dict[int, str] = {
    1: '現実世界に一意にしか存在しない、単一の地点・施設を指す固有名詞。'
    '同名の店舗・施設が他の場所にも複数展開されている場合（チェーン店・フランチャイズ）は'
    'ここに含めず2(poi_brand)にする。知らない店舗名・施設名であっても、'
    '一意の場所を指す固有名詞として成立していればここに含める。例: "東京タワー", "六本木ヒルズ"',
    2: 'チェーン店・フランチャイズ・企業ブランドなど、同名の拠点が複数箇所に存在するブランド名。'
    '単独の店名として書かれていても、それがチェーン展開されている店であれば必ずこちら。'
    '例: "ローソン", "セブンイレブン", "スターバックス"',
    3: '地点の種別・カテゴリを表す一般名詞（固有名詞ではない）。例: "コンビニ", "バス停", "駐車場"',
    4: '住所表記。市区町村・字・丁目・番地など、行政区分や地番を表す文字列。'
    '番地の数字を伴わない「市区町村＋町名」レベルの表記も含める。例: "常盤32-10", "港区六本木"',
    5: '複数の位置意図が混在した複合クエリ。例: "セブンイレブン 東京駅"',
    6: '表記が崩れており検索クエリとして成立していないもの。例: "常盤32-09セブンイレブン"',
    7: "上記いずれにも当てはまらない、または判定不能なもの",
}


def build_system_prompt() -> str:
    lines = [
        f"- {n}: {CATEGORIES[n]} — {CATEGORY_DESCRIPTIONS[n]}"
        for n in sorted(CATEGORIES)
    ]
    category_list = "\n".join(lines)
    example_numbers = ", ".join(str(n) for n in list(CATEGORIES)[:3])
    return f"""あなたは検索クエリの分類器です。与えられた検索クエリの配列を、以下の7カテゴリのいずれか1つに分類してください。

このクエリログは、タクシー配車事業者のオペレーターが電話で乗客から聞いた場所の
説明を検索APIにクエリ化したものです。乗客本人の入力ではなく、オペレーターが
聞き取った内容を解釈・言い換えてクエリ化し、検索結果を見ながら会話で場所を
絞り込んでいく運用のため、生の音声認識結果のような崩れ方とは性質が異なります。

{category_list}

判定の優先順位: 7(others)は「本当にどれにも当てはまらない場合」の最終手段であり、
自信が持てないことを理由に安易に選ばない。固有名詞らしき文字列はまず1(poi)に
当てはまらないか、数字を伴う地番パターンを含む文字列はまず4(address)に
当てはまらないかを優先的に検討すること。知らない・聞いたことがない名前だからと
いって7(others)に倒さない。

1(poi)と2(poi_brand)の判定基準: 同名の店舗・施設が複数箇所に展開されている
（チェーン店・フランチャイズ・全国/地域展開の企業ブランド）と判断できる場合は、
単独の店名としてクエリに書かれていても必ず2(poi_brand)にする。1(poi)は
「その名前を持つ場所が現実には1箇所しか存在しない」固有名詞のみに限定すること。

境界事例（1と2の判定に迷う場合の指針）: ブランド名の後ろに支店名・地名・「店」が
付加されているクエリ（例: "ローソン浜松高塚店", "セブンイレブンこまち店",
"すき家 段原店"）は、ブランド名部分だけで独立した企業・フランチャイズとして
認識できるなら、支店名が付いていても必ず2(poi_brand)。逆に、業種を表す接尾辞
（歯科・内科・医院・ホテル・美容室等）が付いていても、それ単体では全国/広域チェーン
として認識できない地域固有の屋号（例: "土居田町ほりうち歯科", "唐津ホテル"）は
1(poi)のまま。判断軸は接尾辞の種類ではなく、ブランド部分の独立した企業性の有無。

出力は必ず、入力配列と同じ順序・同じ要素数のJSON配列のみを返してください。
各要素は上記カテゴリ番号（1〜7の整数）のいずれか1つです。カテゴリ名の文字列ではなく、必ず数字で返してください。
説明文やコードフェンスは一切含めず、JSON配列のみを出力してください。
例: [{example_numbers}]
"""


SYSTEM_PROMPT = build_system_prompt()


def numbers_to_labels(numbers: list) -> list[str]:
    """LLMが返した番号配列をカテゴリ名配列に変換する。
    マッピングにない値（想定外の数字・文字列混入等）は others 扱い。"""
    return [CATEGORIES.get(n, "others") for n in numbers]


def parse_response_text(text: str) -> str:
    """```json ... ``` のようなコードフェンスが付いた場合に備えて除去する。"""
    text = text.strip()
    if text.startswith("```"):
        text = text.strip("`")
        if text.startswith("json"):
            text = text[4:]
        text = text.strip()
    return text
